-- lua/plugins/lsp/bashls.lua

return {
  -- You can add custom settings here if needed
  filetypes = { "sh", "bash" },
  -- on_attach = function(client, bufnr)
  --   -- custom on_attach logic for bashls, if any
  -- end,
  -- settings = { ... }
}
